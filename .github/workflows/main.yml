name: Main workflow

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    uses: aave-dao/github-workflows/.github/workflows/foundry-test.yml@main
    secrets: inherit
